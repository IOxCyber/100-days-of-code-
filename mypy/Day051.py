#Tuples are very similar to lists, except that they are immutable (they cannot be changed). 
#You can access the values in the tuple with their index, just as you did with lists:

words = ("Give", "Me", "More!",)
print(words) #print entire value
#by index
print(words[1])









"""
# list
list =["one", "two"]
# dictionary 
dict ={1:"one", 2:"two"}
 # tuple 
tp =("one", "two")
""" 
